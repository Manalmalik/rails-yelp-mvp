<div>
  <%= link_to 'Add a new restaurant', new_restaurant_path %>
  <h1> Restaurants </h1>
  <% @restaurants.each do |restaurant| %>
    <%= link_to "#{restaurant.name}", restaurant_path(restaurant) %> </h6><br>
  <% end %>
</div>
